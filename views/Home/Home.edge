@component('Components/Layout', { shell: shell })
  @slot('main')
    <section class="hero">
      <div class="container">
        <h1 class="hero__title">{{ t('home.title') }}</h1>
        <p class="hero__subtitle">{{ t('home.subtitle') }}</p>
        <div class="hero__actions">
          <a class="btn btn--primary" href="/jobs">{{ t('home.findJobs') }}</a>
          <a class="btn btn--ghost" href="/jobs/create">{{ t('home.postJob') }}</a>
        </div>
      </div>
    </section>

    <section class="container section">
      <h2 class="section__title">{{ t('home.why.title') }}</h2>
      <div class="cards">
        <article class="card">
          <h3 class="card__title">{{ t('home.why.verified.title') }}</h3>
          <p class="card__text">{{ t('home.why.verified.text') }}</p>
        </article>
        <article class="card">
          <h3 class="card__title">{{ t('home.why.candidates.title') }}</h3>
          <p class="card__text">{{ t('home.why.candidates.text') }}</p>
        </article>
        <article class="card">
          <h3 class="card__title">{{ t('home.why.multilingual.title') }}</h3>
          <p class="card__text">{{ t('home.why.multilingual.text') }}</p>
        </article>
      </div>
    </section>
  @endslot
@end
