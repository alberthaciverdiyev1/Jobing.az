<section class="container auth">
  <h1 class="auth__title">{{t "auth.register.title"}}</h1>

  <form class="form" method="post" action="/register" novalidate>
    <input type="hidden" name="_csrf" value="{{csrfToken}}" />

    <label class="form__field">
      <span class="form__label">{{t "auth.fields.name"}}</span>
      <input
        class="form__input{{#if errors.name}} is-invalid{{/if}}"
        type="text"
        name="name"
        value="{{values.name}}"
        autocomplete="name"
        required
      />
      {{#if errors.name}}<span class="form__error">{{errors.name}}</span>{{/if}}
    </label>

    <label class="form__field">
      <span class="form__label">{{t "auth.fields.email"}}</span>
      <input
        class="form__input{{#if errors.email}} is-invalid{{/if}}"
        type="email"
        name="email"
        value="{{values.email}}"
        autocomplete="email"
        required
      />
      {{#if errors.email}}<span class="form__error">{{errors.email}}</span>{{/if}}
    </label>

    <label class="form__field">
      <span class="form__label">{{t "auth.fields.password"}}</span>
      <input
        class="form__input{{#if errors.password}} is-invalid{{/if}}"
        type="password"
        name="password"
        autocomplete="new-password"
        required
      />
      {{#if errors.password}}<span class="form__error">{{errors.password}}</span>{{/if}}
    </label>

    <button class="btn btn--primary form__submit" type="submit">
      {{t "auth.register.submit"}}
    </button>
  </form>

  <p class="auth__alt">
    {{t "auth.register.haveAccount"}}
    <a href="/login">{{t "auth.login.title"}}</a>
  </p>
</section>
