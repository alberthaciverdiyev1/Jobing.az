@component('Components/Layout', { shell: shell })
  @slot('main')
    <section class="container error-page">
      <p class="error-page__code">{{ statusCode }}</p>
      <h1 class="error-page__title">{{ t('errors.server.title') }}</h1>
      <p class="error-page__text">{{ t('errors.server.text') }}</p>
      <a class="btn btn--primary" href="/">{{ t('errors.backHome') }}</a>
      @if(requestId)
        <p class="error-page__meta">Request ID: {{ requestId }}</p>
      @end
    </section>
  @endslot
@end
